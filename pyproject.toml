[tool.poetry]
name = "mybot"
version = "1.0.0"
description = ""
authors = []

[tool.poetry.dependencies]
python = "^3.10"
pyTelegramBotAPI = "*"
python-telegram-bot = "*"
pyrogram = "*"
requests = "*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
